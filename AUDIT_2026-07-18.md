# App audit — redundant features and bugs

**Date: 2026-07-18.** Full read of `src/` (20 modules), `server.js`, `overpass.js`,
`overpass-lines.js`, `service-worker.js`, `index.html`. Not a re-run of
[`REVIEW_FINDINGS.md`](REVIEW_FINDINGS.md) — that changelist is closed. This is what is
still standing in the code *after* it landed.

Every item is in exactly one section. **§R = redundant** (a feature still using a retrieval
or geometry path that a later one superseded, or dead surface). **§B = buggy** (it computes
or shows something wrong). An item that is both is filed under whichever is the *cause*.

Verification legend:
- **[M]** — **measured**: the geometry or the string was executed against this repo's code
  and the output is quoted below.
- **[V]** — verified by direct read of the exact lines, both sides of the call.
- **[I]** — inferred from the code path; the failing input is stated but was not executed.

The existing suite is **262 passing** (`npm test`) and none of it contradicts anything here
— every §B item is in a gap the tests don't cover, which is why they survived the last pass.

> **Status 2026-07-18 (later the same day):** **B1–B9 (except B10), R2, R3 and R4 are fixed and pushed**, each
> playtested against two live Mumbai boards (a 380 km² city board and a 1,753 km² MMR board)
> with real Places and real Overpass data, not only unit-tested.
> `test/describe-line-step.test.mjs` closes the gap that let them survive (suite now **267**).
> Everything else below still stands. The same commit also makes hand-drawing a
> *first-class* choice on the §B-adjacent line cards (Coastline, borders, high-speed rail)
> rather than a fallback reachable only when OSM sourcing fails — see the note under §R2.

> **The theme, unchanged from §A:** none of the §B items throw. The error banner in
> `layers.js:198` only fires on a throw, and the `failedSteps` marking only fires on a
> throw or a union failure. Everything below renders as a healthy board.

---

## Summary

| | item | where | severity |
|---|---|---|---|
| **R1** | Transit layer (🚆) is the superseded source, still on by default | `features.js:36`, `app.js:195` | high |
| **R2** | ~~Matching ▸ Transit Line is hand-drawn; `candidateLines` exists~~ — **FIXED 2026-07-18** (Street stays drawn, deliberately) | `questions.js:47,49`, `layers.js:1113` | high |
| **R3** | ~~Two contradicting answers to "what is a 1st/2nd division"~~ — **FIXED 2026-07-18** | `places.js:103`, `overpass-lines.js:97` | high |
| **R4** | ~~Matching ▸ admin1–4 hand-traced against a DDS helper that stops at 3~~ — **FIXED 2026-07-18** | `layers.js:1213`, `places.js:103` | medium |
| **R5** | Socket.IO multiplayer relay — no client, still shipped and listening | `server.js:180-250` | medium |
| **R6** | `outbox` IndexedDB store — Phase 13 leftover, never read or written | `db.js:41` | low |
| **R7** | `CATEGORIES` export — the pre-cards category list, nothing imports it | `places.js:7` | low |
| **R8** | Six dead exports kept alive by nothing | various | low |
| **R9** | Maps JS loads `drawing` + `visualization`, both unused | `maps.js:8` | low |
| **B1** | ~~Metro Lines question renders as `closest "?"` in the panel~~ — **FIXED 2026-07-18** | `tools.js:790` | high |
| **B2** | ~~One-line nearest-line question: no-op, or eliminates the board~~ — **FIXED 2026-07-18** | `layers.js:1128`, `tools.js:446` | high |
| **B3** | ~~Candidate index 0 is pre-checked, so "explicit pick" never binds~~ — **FIXED 2026-07-18** | `layers.js:513` | high |
| **B4** | ~~A failed zone union nulls the whole board, silently~~ — **FIXED 2026-07-18** | `zones.js:129` | high |
| **B5** | ~~Measure mode and every pick/draw flow share the map click~~ — **FIXED 2026-07-18** | `features.js:28` | medium |
| **B6** | ~~`clearBoard` leaves `redoStack` and `railFilter` behind~~ — **FIXED 2026-07-18** | `store.js:~730` | medium |
| **B7** | ~~Rail lines are not re-rendered when the board changes within a game~~ — **FIXED 2026-07-18** | `lines.js:553`, `app.js:180` | medium |
| **B8** | ~~`/overpass` accepts a bbox it never validates~~ — **FIXED 2026-07-18** | `server.js:76` | medium |
| **B9** | ~~`_chooseTentacleLines` and `_chooseTentacle` disagree on default selection~~ — **FIXED 2026-07-18** (by B3) | `layers.js:1386` vs `1438` | low |
| **B10** | `db.js` header says five stores; there are seven | `db.js:3-11` | low |

---

# §R — Redundant: still on the superseded path

### R1. The Transit layer is the thing §G0 exists to replace, and it is on by default **[V]**
`src/features.js:36`, wired at `src/app.js:195-196`.

`lines.js:4-8` and `overpass-lines.js:7-10` both state the finding plainly: Google's
`TransitLayer` is raster tiles from Google's own feed inventory, it takes no options, and in
Mumbai it draws the Metro but **not** the suburban locals — the lines that actually decide
the game. That is the entire reason §G1 built the Overpass line pipeline.

The app then turns that superseded layer on at boot (`features.setTransit(true)`, with the
comment "hiders often stay near transit, so seekers want it visible from the start") and
leaves the real one (🚄 Rail) behind a button that must be pressed, waits on a slow fetch,
and fails ~64% of individual Overpass calls.

So the default view is the incomplete raster one, and the complete vector one is opt-in.
Worse, when both are on they draw the same corridors twice in different colours with no
indication that one of them is missing agencies.

**What to decide:** either default 🚄 Rail on (and accept the fetch at board-open), or keep
🚆 Transit as the instant-but-incomplete preview and say so in its tooltip. What is not
defensible is the current state, where the known-incomplete source is the one a player sees
without asking.

---

### R2. Matching ▸ "Transit Line" and "Street or Path" are still hand-drawn **[V]**
`src/data/questions.js:47,49` → `mode: "nearestLine"`, no `lineKind`.
`src/layers.js:1113` `_matchNearestLine` → `this._drawShape(2, …)` in a loop.

This is `REVIEW_FINDINGS.md` §F4, which that doc marks "superseded by G1" — but only the
*Tentacles* half was actually rebuilt. Tentacles ▸ Metro Lines carries `lineKind: "metro"`
and calls `candidateLines()` for real OSM geometry (`layers.js:1321-1335`). The Matching
Transit Line card never got the same treatment, so it still asks the seeker to trace each
line with a finger.

Everything needed is already built and already exported:
- `lines.js:381` `candidateLines(kind, gameArea, center, radius)` returns
  `{ lines, hidden }` with `paths`, `label`, `key`.
- `tools.js:363` `lineCells()` already accepts **both** shapes — `linePaths()`
  (`tools.js:323`) normalises hand-drawn `coords` and sourced `paths` into one partition.
  The comment there says exactly that: "Normalising here lets both share one partition."
- `layers.js:1366` `_chooseTentacleLines` is the picker, already written.

So the geometry engine is source-agnostic and the Matching card is the only caller still
feeding it fingertips. The cost is the one the coastline commit already named: a traced line
"decides real eliminations at whatever accuracy a fingertip managed", and how good that
accuracy is varies by how well the player knows the city — the exact local advantage §G1
set out to remove.

"Street or Path" is a genuine exception (no worldwide-valid OSM query for "the streets a
player would name"), so it should stay hand-drawn. "Transit Line" has no such excuse — it is
`candidateLines("rail", …)`.

---

### R3. Two different, contradicting definitions of "1st / 2nd admin division" ship together **[V]**

**Definition A** — `src/places.js:103-107`, `DDS_ADMIN_LEVELS`, used by the 🗺 Admin check
tool (`layers.js:795`) and by `extractAdminLevels` (`places.js:131`):

```
administrative_area_level_1  →  "1st Admin division"
administrative_area_level_2  →  "2nd Admin division"
locality                     →  "City / locality (≈3rd)"
```

**Definition B** — `overpass-lines.js:97+`, `COUNTRY_DIVISION_LEVELS`, used by Measuring ▸
1st/2nd Admin Division Border via `divisionOrdinal` (`lines.js:462`):

> Singapore `[5, …]`, Ireland `[5, 6]`, Hong Kong `[5, 6]`, Japan `[4, 7]`, Portugal `[6]`,
> Germany `[4]` (no 2nd division at all), USA `[4]`, UK `[4]`, Philippines — omitted
> entirely.

Definition B is the one the repo *measured*, over 1100 grid probes plus 271 city probes,
and the commit log is a record of it overturning the naive answer four separate ways
(maritime boundaries, territorial water, border straddle, city-states). Definition A is
Google's naming convention, assumed to mean the same thing.

They disagree on real boards. In Japan, Admin check's "County / 2nd admin" row reads
Google's `administrative_area_level_2`, while the Measuring border card correctly draws
level 7 (municipality). In Singapore, Admin check reports a level-1 that the measurement
says is level 5. In Germany the border card says *there is no 2nd division* and falls back
to hand-drawing, while Admin check confidently prints a ✓ or ✗ for one.

A seeker who uses 🗺 Admin check to reason about a division question and then asks it is
being told two different things by the same app. The tool is a "diagnostic, not an
elimination" (`layers.js:790`), which limits the blast radius — but a wrong diagnostic that
contradicts the app's own elimination is worse than no diagnostic.

**Fix direction:** Admin check should resolve its 1st/2nd rows through
`loadCountryDivisions()` / `COUNTRY_DIVISION_LEVELS` like the border cards do, and label
rows it cannot resolve as "–" rather than borrowing Google's numbering.

---

### R4. Matching ▸ admin1–admin4 hand-trace, helped by a DDS layer that only reaches 3 **[V]**
`src/layers.js:1180` `_matchRegion`, `layers.js:1213` `_adminTracePrompt`, `places.js:103`.

The four admin-division Matching cards are `mode: "region"` — the seeker outlines their own
division by hand. The tracing helper offers to highlight the official boundary via DDS, but
`DDS_ADMIN_LEVELS` has exactly three entries, and the code says why:

> "Google's Data-Driven Styling only exposes FeatureLayers for these (there is no level-3/-4
> FeatureType), so the tracing helper can only draw these boundaries even though the game
> asks about 1st–4th divisions."

So on the 3rd and 4th Admin Division cards, the helper opens, offers levels that are *not
the one being asked about*, and the seeker traces the wrong outline or none at all. And the
whole path is gated on a Map ID with boundary FeatureLayers enabled — which, per
[`MEMORY`](.) and `boundaries.js:_highlightFeature`, is a Cloud-console state this repo
cannot set, so in practice it degrades to a plain draw prompt.

Meanwhile `lineGeometry(kind, gameArea, { divisionOrdinal })` (`lines.js:462`) already
returns real boundary geometry for ordinals 1 and 2 from OSM, worldwide, with no Map ID and
no DDS.

**Fix direction:** route admin1/admin2 Matching through `divisionOrdinal` the way the
Measuring border cards do — the region can be derived from the boundary rather than traced.
admin3/admin4 have no measured level table and should stay hand-drawn, but their trace
helper should say "no official outline available for this level" instead of offering the
wrong three.

---

### R5. The Socket.IO multiplayer relay is live in production and has no client **[V]**
`server.js:180-250`, plus the `socket.io` runtime dependency and the `SocketIOServer`
import at `server.js:19`.

`REVIEW_FINDINGS.md` closes §E4 as "multiplayer, out of scope" and `SESSION_LOG.md` §1.2
notes that setting `MULTIPLAYER_URL` "also re-enables the parked Phase 13 multiplayer
relay, but that is out of scope". Nothing in `src/` imports a socket client — grep for
`socket`, `session.join`, `MULTIPLAYER_URL` across `src/` returns nothing.

What ships is an unauthenticated relay that accepts `session.join` with any `code`, stores
the first joiner as host, and caches whatever `snapshot` payload any member emits in
process memory. `sessions` is an unbounded `Map`; rooms are GC'd only when every member
disconnects cleanly.

It is not exploitable for much — there is no data in it that anyone else put there — but it
is an open write surface on a public host, serving a feature with no consumer, on the same
process the Overpass proxy needs to stay responsive on.

**Fix direction:** delete the relay block and the `socket.io` dependency, or gate it behind
`process.env.MULTIPLAYER_ENABLED`. Reviving it later is a git revert.

---

### R6. The `outbox` object store is a Phase 13 leftover **[V]**
`src/db.js:41-43`, declared at `DB_VERSION 3`.

"offline-queued multiplayer events (Phase 13)". Never `get`, `put`, `getAll` or `del`'d
anywhere. It cannot be removed without a `DB_VERSION` bump, so leaving it is defensible —
but it should be labelled dead rather than pending, so the next reader doesn't go looking
for the queue that fills it.

---

### R7. `CATEGORIES` in `places.js` predates the question bank **[V]**
`src/places.js:7-18`.

Ten `{id,label,type}` entries. Nothing imports it — `layers.js:10` imports only
`searchCategoryResilient`, `reverseGeocode`, `searchText`, `adminDivisionsAt`, `matchNames`.
It is the category list from before `data/questions.js` became the source of truth
(`questions.js:2`: "Source of truth: docs/jetlag_questions.md. Each tool exposes ONLY these
items"). It now duplicates a subset of `MATCHING` / `MEASURING` with different ids, so
editing it appears to change the cards and does not.

---

### R8. Six exports with no caller **[V]**

Grepped across `src/`, `test/`, `scripts/`, `*.html`, excluding `node_modules`:

| export | file | note |
|---|---|---|
| `distancePointToArea` | `tools.js:591` | written to "bound which candidate places are relevant to a tentacle radius"; `_chooseTentacle` filters by `turf.distance` to the seeker instead (`layers.js:1354`) |
| `centroidOfRing` | `geo.js:220` | superseded by `_adminRefPoint`, which centroids the assembled area, not a ring |
| `LINE_KIND_LABEL` | `lines.js:46` | the sheet titles use `card.label` from `questions.js` |
| `Boundaries.hasOverlays` | `boundaries.js:175` | nothing asks; `clear()` is unconditional |
| `MapFeatures.isTransitOn` | `features.js:47` | the toolbar tracks state via the return of `toggleTransit()` |
| `timer.isRunning` | `timer.js:48` | `startCountdown` calls `stopCountdown` unconditionally, so nothing needs to ask |

None are harmful. They are listed because each one reads as an available helper, and two of
them (`distancePointToArea`, `centroidOfRing`) look like the *right* helper for code that
now does the job inline — the next change to that code is likely to call the dead one.

---

### R9. Two Maps JS libraries are loaded and never used **[V]**
`src/maps.js:8` — `["maps", "places", "geometry", "drawing", "marker", "visualization"]`.

- `drawing` — `zones.js:30` says outright: "the old DrawingManager was removed from the Maps
  JS API in v3.65", and zones are drawn with a custom click tool. Nothing references
  `google.maps.drawing`.
- `visualization` — no heatmap or any other visualization primitive anywhere.

`geometry` **is** used (`features.js:146`, `computeDistanceBetween`), `places` and `marker`
obviously so. Dropping the two dead ones is a smaller first payload on a phone, which is the
target device.

---

# §B — Buggy

### B1. A Metro Lines question always reads `closest "?"` in the Questions panel **[M]**

> **Measured 2026-07-18.** `describeStep` on a Metro Lines step built exactly as
> `layers.js:1413` builds it returns:
> ```
> Tentacles · Metro Lines · closest “?” (25 km)
> ```


`layers.js:1413` stores a line-mode tentacles step as:

```js
const inputs = { category: cat.id, categoryLabel: cat.label, radius: cat.radius,
                 lines: stored, center };
```

`tools.js:785-791` describes every tentacles step as:

```js
const sel = (step.inputs.features || [])[step.answer?.featureIndex];
return `Tentacles · ${cat} · closest “${sel?.name || "?"}” (${rTxt})`;
```

`inputs.features` is `undefined` for a line step, so `sel` is `undefined` and every Metro
Lines question in the panel reads **`Tentacles · Metro Lines · closest "?" (25 km)`** —
identical for every line the seeker could have picked.

`tools.js:503` already knows the shape (`const isLines = Array.isArray(lines) && lines.length > 0`);
`describeStep` was simply never taught it. The `matching` branch immediately above
*was* — `tools.js:778-781` handles `nearestLine` by looking up `inputs.lines`.

**Impact:** the panel is how a seeker decides which question to disable when the board looks
wrong. With several Metro Lines questions asked, they are indistinguishable, and the rename
default (`layers.js:782` seeds `promptText` with `describeStep(step)`) carries the `"?"`
into the name if they rename it.

**Fix:** mirror the matching branch —

```js
if (step.answer?.none) return `Tentacles · ${cat} · none within ${rTxt}`;
const items = step.inputs.lines || step.inputs.features || [];
const sel = items[step.answer?.featureIndex];
return `Tentacles · ${cat} · closest “${sel?.label || sel?.name || "?"}” (${rTxt})`;
```

(`lines` entries carry `label`, `features` entries carry `name` — hence both.)

---

### B2. A nearest-line question with ONE line is accepted, and does nothing **[M]**

> **Measured 2026-07-18** on a 195.6 km² board with one drawn line through it:
> ```
> match=true  -> eliminated null      of 195.6 km2   (silent no-op)
> match=false -> eliminated 195.6 km2 of 195.6 km2   (whole board)
> ```
> Neither throws, so neither is marked failed or banner'd. The "differ" case is the more
> alarming of the two: the seeker answered correctly and the board goes fully eliminated,
> which surfaces as "check the most recent answer" — advice pointing away from the cause.


`layers.js:1128`: `if (lines.length < 1) { … return this.openPanel(); }` — so **one** drawn
line proceeds to the selection sheet and is committed.

Follow it through `tools.js:446` `matchingNearestLine` → `lineCells(lines, gameArea)`:
with a single line, every densified seed has `owner = [0]`, so `perOwner[0]` collects every
Voronoi cell, `turf.union` merges them into the whole padded bbox, and
`cells[0] = safeIntersect(merged, gameArea)` is **the entire game area**.

Then:
- answer *match* → `safeDiff(gameArea, gameArea)` → `null` → eliminates nothing. The
  question is committed, enabled, listed in the panel, and contributes zero shading. It does
  not throw, so it is never added to `failedSteps` and never appears in the banner.
- answer *differ* → `eliminated = cells[0]` = the whole board → the board goes fully
  eliminated, `computeActiveArea` returns `EMPTY_AREA`, and the seeker gets "Every question
  combined rules out the whole area — check the most recent answer" for a question that was
  answered correctly.

Both outcomes are wrong and neither names the cause. The Tentacles line picker already got
this right — `layers.js:1395` warns when `lines.length < 2` that the question "can't
distinguish between lines" — because there the radius still makes a 1-line question
meaningful (it degenerates to a radar). In *Matching* there is no radius, so a single line
is not degenerate-but-useful, it is meaningless: "is your nearest line the same as mine"
when there is only one line has exactly one possible truthful answer.

**Fix:** require `minPts`-style two lines in `_matchNearestLine` — change the guard to
`lines.length < 2` with a toast saying why, matching how `_matchNearest` enforces
`minCount: 2` (`layers.js:1008`).

---

### B3. The candidate radio list pre-checks index 0, so the "require an explicit pick" guard rarely binds **[V]**

`layers.js:513` builds every feature radio list with:

```js
<input type="radio" name="${name}" value="${i}" ${i === 0 ? "checked" : ""}/>
```

Two `Add` handlers then guard against a missing selection, each with a comment describing
the bug it is there to prevent:

- `layers.js:1042` — "Require an explicit pick. Defaulting to `0` here was the second mouth
  of the same bug: with the checked item filtered away, Add silently recorded a station the
  seeker never chose and could not see."
- `layers.js:1463` — same reasoning for Tentacles.

But `find(r => r.checked)` only returns `undefined` when *something cleared the default* —
which is `repairRadioSelection` (`ui.js`), and that only runs when the user typed in the
filter box (`_wireFeatureSearch`, `layers.js:531`). On the common path — open the sheet,
scan a list of six stations, press Add without touching the filter — **candidate #1 is
already checked and is silently committed.**

That is the original failure with a narrower mouth, not a closed one: the seeker never made
a choice, and `describeStep` will report a station name they did not pick.

The fix the code already believes it made is one character: drop `${i === 0 ? "checked" : ""}`
from `_featureListHTML`. The guards downstream are then load-bearing on every path, and
`repairRadioSelection`'s "otherwise clear it and require an explicit pick" branch becomes
the whole rule rather than a special case.

Note `_chooseTentacleLines` (`layers.js:1386`) already builds its list with **no** default
checked — so the two pickers behave differently today (see B9).

---

### B4. A failed zone union silently deletes the play area **[V]**
`src/zones.js:129` and `:143`:

```js
store.update((g) => {
  g.zones.push(zone);
  g.gameArea = unionRings(g.zones.map((z) => z.polygon));
});
```

`unionRings` (`geo.js:135-146`) catches its own exception and returns `null`. So a turf
union failure on the *newly added* zone assigns `gameArea = null` — throwing away a board
that was valid a moment ago, with every zone still listed in the panel.

Downstream, `layers._render` (`layers.js:271`) does handle a null `gameArea` gracefully and
raises "N questions are saved but can't be shown without a play area — add a zone (Zones ▸
Draw) to see them again". That message is *wrong here*: the seeker has zones, they just
added one, and the advice is to do the thing that broke it.

The same shape in `removeZone` (`:143`) is less severe (removing a zone genuinely can empty
the board) but is indistinguishable from the failure case.

**Fix:** compute into a local, and only commit when it succeeds:

```js
const merged = unionRings(g.zones.map((z) => z.polygon));
if (!merged && g.zones.length) { /* keep the old gameArea, toast the failure */ }
else g.gameArea = merged;
```

D1/D2 already refuse the two *known* causes of a bad ring at draw time, which is why this
has not been seen — but `unionRings` returning null is exactly the "swallowed, then
assigned" pattern that `computeActiveArea` was fixed for in the last changelist
(`tools.js:744-754`), and it is still here in the zone layer.

---

### B5. Measure mode competes with every pick and draw flow for the map click **[V]**

`features.js:28` registers a permanent map click listener in `init()`:
```js
this.map.addListener("click", (e) => this._onClick(e.latLng));
```
`_onClick` returns early unless `measure.active`, so it is inert *while measure is off*.

But nothing turns measure off when a question flow starts. `layers.pick()`
(`layers.js:395`), `layers._drawShape()` (`:447`) and `zones.startDraw()` (`zones.js:44`)
each add their **own** map click listener. With 📏 Measure toggled on, tapping the map to
place a radar centre also drops a numbered measure marker and, on the second tap, draws a
yellow measurement line and shows the readout — over the question being set up.

Worse in the other direction: `_onClick` calls `this._clearMeasure()` when two points already
exist, so a long drawing session leaves markers appearing and vanishing under the drawn
polygon.

`features.clearAll()` (`:95`) does reset measure — but it is only called on a *game switch*
(`app.js:185`) and on Clear board (`games.js:~128`), never when a tool flow begins.

**Fix:** have `pick`, `_drawShape` and `startDraw` suspend measure mode for their duration
(the same way they set `draggableCursor: "crosshair"` and restore it in `cleanup`), or have
`_onClick` bail whenever a `.draw-bar` is present in the DOM — that element is the existing,
already-unique signal that a map-tap flow owns the click.

---

### B6. `clearBoard` leaves the redo stack and the rail filter behind **[V]**
`store.js` `clearBoard()`:

```js
current.zones = []; current.gameArea = null; current.history = [];
current.focusZone = { point: null, radius: null };
```

Not cleared:
- **`redoStack`** — still holds ids of steps that no longer exist. `Layers.redo()`
  (`layers.js:176-185`) handles this correctly (it pops until it finds a live step, then
  toasts "Nothing to redo"), so the consequence is cosmetic: `canRedo()` returns `true`
  (`layers.js:186` checks only `length`), the ↷ Redo button renders **enabled** on a freshly
  cleared board, and pressing it does nothing but toast.
- **`railFilter`** — "we're only playing on the Blue and Red lines" is explicitly a property
  of *this board* (`model.js`, railFilter comment). Clear board wipes the board and keeps the
  filter, so the next board silently inherits hidden modes and hidden line keys from a game
  that no longer exists. Since `hiddenLines` keys are `"subway:1"`-style and cities reuse
  those refs, a filter from a Berlin board can hide a line on a Paris one.

`createGame` gets both right; only the in-place clear path drifted.

---

### B7. Rail lines are drawn once and never re-rendered within a game **[V]**

`Lines.render()` (`lines.js:553`) is called from exactly three places: `openPanel`
(`:593`), `_setFilter` (`:582`), and the mode/line checkbox handlers. Unlike `Zones`,
`Layers` and `Focus`, the `Lines` class **never subscribes to the store** — compare
`zones.js:35`, `layers.js:126`, `focus.js` (`store.subscribe(() => this.render())`).

Consequences within a single game:
- The payload is fetched once for the board bbox at the moment 🚄 was first pressed
  (`lines.js:526`: `if (this.data) return this.data;`). Add or remove a zone afterwards and
  the board changes, but the rail geometry does not — it still shows the old bbox's lines,
  now mismatched against the new play area, with no refetch and no notice.
- Palette changes (`jltg:palette`) restyle zones, layers and guides live but not the rail
  strokes — those use `MODE_COLOUR` from `ROUTE_MODES` (`lines.js:35-42`), which is not
  palette-driven at all. On the colour-blind palette every other overlay switches to
  Okabe-Ito and the rail lines stay on the default vivid hues, which is the one place a
  colour-blind player most needs the distinction (five modes, five colours).

The game-switch case *is* handled (`app.js:186-189` calls `lines.clear()`), which is why
this reads as an oversight in the within-game case rather than a missing concept.

---

### B8. `/overpass` never validates its bbox, while `/overpass/lines` does **[V]**

`server.js:76-83` parses four numbers and reassembles the string:

```js
const parts = String(req.query.bbox || "").split(",").map(Number);
if (parts.length !== 4 || parts.some((n) => Number.isNaN(n))) return res.status(400)…
const [s, w, n, e] = parts;
const bbox = `${s},${w},${n},${e}`;
```

That is the only check. A reversed bbox (`n < s` or `e < w`), or one outside ±90/±180, goes
straight into `buildQuery` and Overpass returns **HTTP 200 with zero elements** — which the
client cannot distinguish from "this area genuinely has no hospitals". In
`searchCategoryResilient` (`places.js:392`), an empty OSM answer for a dense category falls
through to Google without comment, so the seeker gets Google's capped 60 and no sign the OSM
query was malformed.

`bboxIsValid` already exists (`overpass-lines.js`, imported at `server.js:20`) and is used by
`/overpass/lines:118` for exactly this — with the message "bbox must be S,W,N,E with S<N,
W<E and within ±90/±180." The older route was never brought up to it.

Reaching this needs a malformed client bbox, which `areaBboxSWNE` / `discBboxSWNE`
(`places.js:320`, `:335`) do not currently produce for a valid board — so **[I]** on the
trigger, **[V]** on the missing guard. It is one line, and it converts a silent empty
result into a named 400.

---

### B9. The two candidate pickers disagree on whether anything starts selected **[V]**

- `_chooseTentacle` (points) uses `_featureListHTML`, which checks index 0 (`layers.js:513`).
- `_chooseTentacleLines` (Metro Lines) builds its own list at `layers.js:1386` with **no**
  `checked` attribute, and neither does its "None — a miss" row.

So the same 🐙 Tentacles card, answered about lines vs about points, behaves differently:
one arrives with an answer pre-filled, the other requires a tap. The line version's guard
(`:1407`, "Choose which line they're closest to, or None for a miss") fires as designed; the
point version's identical guard (`:1464`) usually cannot.

Filed separately from B3 because fixing B3 as described (drop the default `checked`) also
resolves this — the two lists converge on the line version's behaviour, which is the correct
one.

---

### B10. `db.js` documents five stores and creates seven **[V]**
`src/db.js:3-11` — the header says "Database `jltg` with five stores" and then lists
`games`, `zones`, `categories`, `pins`, `outbox`, `lines`, `settings`. Seven. The count was
never updated as Phase 9 (`categories`, `pins`), Phase 13 (`outbox`) and §G1 (`lines`) each
added one.

Trivial, but `db.js` is the file someone reads to find out what persists, and the first
sentence is wrong.

---

## What was checked and found sound

Recorded so a later pass doesn't re-derive it:

- **Antimeridian and self-intersection guards** (`geo.js:61`, `:89`) are applied at all four
  entry points — zone draw (`zones.js:99,104`), zone import (`zones.js:160,161`), and ring
  draw (`layers.js:460,466`). No path into `unionRings` or `ringToPoly` bypasses them.
- **`EMPTY_AREA` vs `null`** (`tools.js:24`) is discriminated by its only consumer
  (`layers.js:221-238`), including the `maskEverything` branch. A1 is genuinely closed.
- **Shared-track ownership** in `lineCells` (`tools.js:363-442`) — the one-seed-per-coordinate
  model and the merge-once-then-clip order both match what the comments claim, and the
  overlap is conservative in the direction stated.
- **Service-worker shell list** (`service-worker.js:6-32`) covers all 20 `src/` modules
  including `data/questions.js`. Network-first with a cache fallback, and Google/`config.js`
  correctly excluded.
- **Distance-unit handling** (`ui.js`, `distanceFieldHTML` / `readDistanceMeters`) converts
  at the UI boundary only; every caller stores metres. No caller clamps a bad value.
- **`prepareImport`** (`model.js`) strips the id on every import path; `duplicate()`
  (`games.js`) does the same by destructuring. Neither can overwrite an existing game.

---

## Suggested order

1. **B1, B3, B9** — one small edit each, all three are "the app records or reports an answer
   the seeker did not give". B3 is the one that can corrupt a live game.
2. **B2, B4** — the two remaining silent-no-op / silent-loss paths.
3. **R2** — the largest real win, and the machinery is already written and already tested.
4. **R3, R4** — resolve the app to one definition of a division.
5. **R1** — a product decision, not a code fix; make it deliberately.
6. **B5, B6, B7, B8** — contained correctness.
7. **R5–R9, B10** — deletion and documentation.
