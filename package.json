{
  "name": "jltg-hide-and-seek",
  "version": "1.0.0",
  "private": true,
  "description": "JLTG Hide & Seek map companion — backend (Overpass proxy).",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "build:config": "node scripts/build-config.js",
    "test": "node --test \"test/*.test.mjs\"",
    "cap:sync": "cap sync android",
    "cap:open": "cap open android"
  },
  "dependencies": {
    "express": "^4.22.2",
    "socket.io": "^4.7.5"
  },
  "//devDependencies": "Phase 39 (Android shell). Native-build-only — NOT needed for `npm test`. Run `npm install` before the Android build; see docs/ANDROID_BUILD.md.",
  "devDependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  }
}
